# MCP 服务安全治理建议

## 使用前提

本项目中的 MCP 服务可能由境外个人、组织或云平台维护，代码托管、依赖下载、容器镜像、API 端点和数据处理位置可能不在中国境内。服务所在地、运营主体、数据流向、跨境传输机制、留存期限和删除能力不能仅凭项目名称或仓库位置推断，必须由使用方在接入前核验。

目录收录不构成安全认可、合规认证或安装建议。任何服务在下载、安装、升级或接入生产环境前，都应完成来源审查、版本固定、SBOM 登记、恶意代码和依赖扫描、权限审查、数据出境评估、隔离测试与责任人审批。

## MCP 物料清单（SBOM）

MCP 服务应作为软件供应链资产进行登记。每个服务分配唯一资产编号，并将源码仓库、提交哈希、发布包、依赖锁文件、容器镜像摘要、配置文件、扫描报告和审批记录关联到该编号。生产环境不应直接使用 `latest`、浮动分支、未锁定的 npm/PyPI 依赖或未经校验的安装脚本。

建议至少记录以下字段：

| 字段组 | 建议字段 |
|---|---|
| 身份 | 资产编号、服务名称、业务用途、负责人、环境、数据分级 |
| 来源 | 上游仓库、发布地址、维护者、许可证、下载时间、来源区域、供应商 |
| 版本 | 版本号、Git 提交哈希、包完整性哈希、容器镜像 digest、锁文件哈希 |
| 依赖 | npm/PyPI/Maven/系统依赖、依赖版本、已知漏洞、许可证冲突 |
| 接口 | MCP 传输方式、工具列表、API 端点、域名、端口、OAuth 范围、所需密钥 |
| 数据 | 输入数据类型、输出数据类型、是否含个人信息、是否含源代码、处理区域、留存期限 |
| 控制 | 沙箱、文件目录、网络策略、资源限制、日志位置、人工审批、最近复核时间 |
| 结论 | 风险等级、例外编号、审批人、上线日期、下线日期、升级条件 |

SBOM 应在首次接入、版本升级、依赖变化、镜像变化、API 端点变化、权限变化或安全事件后重新生成和复核。删除服务时，应同步撤销凭据、删除缓存和报告、关闭网络策略，并保留必要的审计记录。

## 服务接入门闸

### 第一闸：来源与完整性

核验维护者、仓库历史、许可证、发布渠道、版本签名、提交哈希、包哈希和容器 digest。禁止执行未审查的远程安装脚本；依赖和镜像应通过可信代理或内部制品库获取。发现仓库突然转移、维护者不明、发布包与源码不一致、混淆代码、可疑安装脚本或高危漏洞时，应暂停接入。

### 第二闸：能力与权限

逐项梳理 MCP 工具能够执行的动作，特别关注命令执行、任意文件读写、网络请求、消息发送、PR 评论、应用安装、设备控制、样本上传、凭据读取和数据删除。默认拒绝未知工具，采用服务白名单、工具级白名单、最小文件目录、最小 API 范围和短期凭据；高风险工具不得默认自动批准。

### 第三闸：运行环境与数据流

在隔离账号、容器或虚拟机中进行动态测试，限制出站域名、端口、DNS、文件系统和系统调用，设置 CPU、内存、并发和请求大小上限。记录输入、工具调用、返回结果、错误、网络连接和管理员操作。对境外或第三方云端点，确认是否会上传代码、聊天消息、APK/IPA、漏洞报告、恶意样本、威胁情报或个人信息；无法确认时禁止上传敏感数据。

### 临时安全沙箱建议

对于逆向工程、APK/IPA 分析、恶意样本处理、渗透测试、命令执行、不可信代码扫描、依赖构建和会执行第三方脚本的 MCP 服务，建议优先使用 E2B 等临时安全沙箱或组织批准的等效隔离环境。E2B 官方文档说明其 Sandbox 可按需创建隔离 Linux 虚拟机，并支持设置超时、暂停、恢复和主动销毁；这些能力适合构建短生命周期的分析环境，但不能替代组织自身的网络隔离、凭据控制、数据分级和审计措施。[1] [2]

沙箱配置应采用一次性或可复现模板、短超时、最小权限、无生产凭据、出站网络白名单、只读输入挂载、受控输出目录、CPU/内存/进程/磁盘配额和完整命令审计。分析任务结束后主动销毁沙箱和临时文件，不应默认暂停并长期保留源码、样本、密钥或报告。需要访问 iOS/Android 模拟器、真实设备或内网系统时，应将设备控制面与样本执行面分离，并通过代理和白名单限制交互。

E2B 或其他第三方沙箱本身也属于外部服务，应在 SBOM 中记录其 SDK、镜像模板、API 端点、区域、凭据、数据留存和版本；涉及境外或第三方云端处理时，应先完成数据出境和供应商评估。对无法确认数据处理位置、隔离边界、销毁机制或日志范围的沙箱服务，不得处理敏感数据。

### 第四闸：审批与持续监控

业务负责人、安全负责人和数据合规责任人应根据数据分级和工具权限完成审批。上线后持续监控依赖漏洞、版本变化、异常网络、工具调用、敏感数据外传、权限提升和错误率。升级必须重新走版本完整性、SBOM、漏洞和回归测试流程。

## 建议的拒绝条件

下列任一情况存在时，不应接入生产环境：无法确认服务来源或版本完整性；依赖、镜像或安装脚本存在未解释的高危风险；服务要求不必要的管理员权限、全盘访问或长期凭据；工具能够执行任意命令但没有沙箱和审计；数据处理位置、第三方共享、留存或删除机制无法确认；服务会自动发送消息、发布评论、修改生产资源或上传文件且没有人工确认；发现提示注入、越权、敏感数据外传或日志泄露；业务、信息安全或合规责任人未完成审批。

## 风险分级建议

| 等级 | 典型特征 | 最低控制要求 |
|---|---|---|
| 低 | 只读、无敏感数据、无网络写操作 | 版本固定、依赖扫描、基础日志 |
| 中 | 读取代码或内部文档，访问受限 API | SBOM、沙箱、白名单、短期凭据、人工审批 |
| 高 | 命令执行、文件写入、消息发送、应用安装、设备控制、样本上传或外部云 API | 隔离环境、工具级审批、出站控制、完整审计、定期复核 |
| 禁止 | 未授权目标、未知数据流、无法审计、恶意行为或绕过门闸 | 不得安装、下载或接入 |

## 事件响应

发现依赖投毒、凭据泄露、越权调用、异常出站、恶意样本扩散或敏感数据外传时，应立即停用服务、撤销令牌、隔离运行环境、保全日志和样本哈希，评估影响范围并按组织事件响应流程处置。恢复前必须完成根因分析、版本重建、SBOM 更新和重新审批。

## References

[1]: https://e2b.dev/docs "E2B Documentation"
[2]: https://e2b.dev/docs/sandbox "E2B Sandbox Lifecycle"
