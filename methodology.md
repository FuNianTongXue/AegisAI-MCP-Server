# 采集方法与限制

## 采集范围

本目录面向四类使用场景：聊天通讯、信息安全、报告生成和代码检测。检索使用 LobeHub MCP 公开详情页及公开搜索结果，优先纳入标题、描述或分类与目标主题直接相关的服务，并记录每个详情页显示的 Identifier、Version、Category、Installs、Connection Type 和 GitHub 上游链接。

## 排序与收录

`Installs` 作为主要热度指标。对于同一主题，按详情页显示的 Installs 从高到低排序；跨主题比较只作为参考，不代表不同类别之间具有完全可比性。本次快照中已核验的最高值为 Code Auditor（157），其次为 Teams MCP Server（76）、Actionlint MCP（47）和 Code Review MCP（46）。

“下载量”在本项目中严格指 **LobeHub 页面公开显示的 Installs**。LobeHub 页面没有提供足够信息证明该数值等于 npm 下载量、GitHub 下载量或独立用户数，因此没有将这些指标混用，也没有为缺失数据进行估算。

## 事实核验

每项记录至少保留一个 LobeHub 详情页链接和一个上游 GitHub 链接。功能摘要根据详情页描述归纳，未将推测能力写成已验证事实。安装、认证、权限和外部依赖来自详情页中明确出现的说明；部署前仍需阅读上游仓库的最新文档和源代码。

## 安全审查原则

通讯服务按消息内容、会话数据、Full Disk Access、OAuth 文件和浏览器会话评估风险。信息安全服务按目标授权、命令执行、容器隔离、漏洞模板和网络暴露评估风险。报告生成服务按远程数据暴露、模板路径和报告留存评估风险。代码检测服务按源代码敏感性、GitHub Token 权限、自动发布评论和外部工具执行评估风险。

> 本项目是选型资料，不是安全认证、渗透测试授权、代码审计结论或法律意见。

## 已知限制

LobeHub 的市场目录会动态变化，Installs 可能随时间增加或因数据清理而变化。公开搜索不能保证覆盖市场中的每一条记录，因此本项目使用“公开可检索且高相关候选”的表述，而不宣称永久穷尽。建议后续通过固定日期运行相同关键词检索并提交变更记录，形成可比较的时间序列。

## 更新建议

后续维护者应保留采集日期、原始页面快照、变化字段和排除原因；对于安装量相同的候选，可按功能完整度、最近版本、源码活跃度和权限最小化程度进行二级排序。任何将服务部署到生产环境的决定，都应先进行依赖扫描、凭据隔离、网络策略审查和人工授权确认。
